// import { render, screen } from "@testing-library/react";
// import App from "./App";

// test("renders link in header", () => {
//   render(<App />);
//   const linkElement = screen.getByText(/15 counter/i);
//   expect(linkElement).toBeInTheDocument();
// });
